import 'package:flutter/painting.dart' show Color;

const fontFamily = 'Open Sans';

const title = 'SGS Automobile GmbH & Co. KG';

const slideSwitchSecs = Duration(seconds: 7);
const slideAnimateMillis = Duration(milliseconds: 500);

const sgsRedColor = Color.fromRGBO(254, 0, 0, 1);
const statusBarColor = Color.fromRGBO(0, 0, 0, 0.85);
const inactiveButtonColor = Color.fromRGBO(189, 189, 189, 1);
