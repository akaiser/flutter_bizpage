import 'package:flutter/material.dart';

const fontFamily = 'Open Sans';

const title = 'SGS Automobile GmbH & Co. KG';

const slideTimerInterval = Duration(seconds: 7);

const sgsRed = Color.fromRGBO(254, 0, 0, 1);
const statusBarColor = Color.fromRGBO(0, 0, 0, 0.85);
